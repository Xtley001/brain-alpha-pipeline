wqb>=0.2.5
psycopg[binary]>=3.1
psycopg_pool>=3.2
# Update 10 Item 6: version-tracked schema migrations, replacing the old
# "execute the whole schema.sql file unconditionally on every cold start"
# non-migration -- see pipeline/db/alembic/ and Repo.migrate()'s
# docstring.
alembic>=1.13
# Update 10 Item 9.1: replaces the hand-rolled pure-Python _pearson() in
# pipeline/filter/correlation_check.py with numpy.corrcoef.
numpy>=1.26
requests>=2.31
google-genai>=0.3
openai>=1.30
python-dotenv>=1.0

# dev / test only
pytest>=8.0
pytest-asyncio>=0.23
